package HW2.HW8.HW9;
//Create an abstract class called Shape with an abstract method called calculateArea().
//        Create two subclasses of Shape called Rectangle and Circle. Each class should implement the calculateArea() method in its own way.
//        Create an array of Shape objects that contains both Rectangle and Circle objects,
//        and call the calculateArea() method on each object in the array.
public abstract class Shape {
    abstract int calculateArea();
}
