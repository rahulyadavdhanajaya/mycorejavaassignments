package HW2.HW8;
//Create a superclass called Person with a method called greet().
  //      Create two subclasses of Person called Student and Teacher, each with their own implementation of the greet() method.
    //    Create an array of Person objects that contains both Student and Teacher objects,
      //  and call the greet() method on each object in the array.
public abstract class Person {
    abstract String greet();

}
